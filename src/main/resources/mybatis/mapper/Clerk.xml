<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.saonan.mapper.UsersMapper">
   <select id="isLogin"  resultType="cn.saonan.pojo.Users" >
   		select username,realname from users where username=#{username} and userpwd=#{userpwd}
   </select>
</mapper>